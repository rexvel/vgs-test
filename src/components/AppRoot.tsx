import React from 'react';
import { Route } from 'react-router-dom';
import Routes from '../routes/Routes';

const AppRoot: React.FC = () => <Route component={Routes} />;

export default AppRoot;
